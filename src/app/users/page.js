import { Box, Typography } from '@mui/material';

const UsersPage = () => {
  return (
    <Box>
      <Typography variant="h1">Users</Typography>
      <Typography variant="body1">
        This is the users page. It will display a list of users and allow you to
        manage them.
      </Typography>
    </Box>
  );
};

export default UsersPage;
